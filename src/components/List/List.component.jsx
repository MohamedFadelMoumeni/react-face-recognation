import React from 'react';
import './List.css';

export const List = ({ dataColor }) => {
    const data = dataColor.map(e => {
        return (
            <div>

            </div>
        )
    })
}
